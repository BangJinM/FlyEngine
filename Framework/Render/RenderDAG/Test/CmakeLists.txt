file(GLOB_RECURSE HEADER_FILES *.h)
file(GLOB_RECURSE HEADER_FILES *.hpp)
file(GLOB_RECURSE SOURCE_CPP_FILES *.cpp *.cc)
file(GLOB_RECURSE SOURCE_C_FILES *.c)

set(ALL_FILES
    ${HEADER_FILES}
    ${SOURCE_CPP_FILES}
    ${SOURCE_C_FILES}
)

link_libraries(DAG)

add_executable(Test1 ${HEADER_FILES} ${ALL_FILES})
