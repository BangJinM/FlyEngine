
set(HEADER_FILES 
IApplication.h
GLFWApp.h
)

set(SOURCE_CPP_FILES 
main.cpp
GLFWApp.cpp
)

set(ALL_FILES
    ${HEADER_FILES}
    ${SOURCE_CPP_FILES}
)

add_library(fly_engine_core_lib ${ALL_FILES})

find_package(glfw3 CONFIG REQUIRED)
target_link_libraries(fly_engine_core_lib glfw)

