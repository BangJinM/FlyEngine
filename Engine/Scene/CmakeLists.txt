file(GLOB_RECURSE HPP_HEADER_FILES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/Scene/*.hpp)
file(GLOB_RECURSE H_HEADER_FILES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/Scene/*.h)
file(GLOB_RECURSE SOURCE_CPP_FILES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/Scene/*.cpp )
file(GLOB_RECURSE SOURCE_C_FILES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/Scene/*.c )
file(GLOB_RECURSE SOURCE_CC_FILES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/Scene/*.cpp )

set(ENGINE_SCENE_FILES
    ${HPP_HEADER_FILES}
    ${H_HEADER_FILES}
    ${SOURCE_CPP_FILES}
    ${SOURCE_CC_FILES}
    ${SOURCE_C_FILES}
)

find_package(rttr CONFIG REQUIRED)
link_libraries(RTTR::Core)