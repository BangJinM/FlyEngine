file(GLOB_RECURSE HPP_HEADER_FILES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/Math/*.hpp)
file(GLOB_RECURSE H_HEADER_FILES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/Math/*.h)
file(GLOB_RECURSE INL_HEADER_FILES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/Math/*.inl)
file(GLOB_RECURSE SOURCE_CPP_FILES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/Math/*.cpp )
file(GLOB_RECURSE SOURCE_C_FILES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/Math/*.c )
file(GLOB_RECURSE SOURCE_CC_FILES CONFIGURE_DEPENDS ${CMAKE_CURRENT_SOURCE_DIR}/Math/*.cpp )

set(ENGINE_MATH_FILES
    ${HPP_HEADER_FILES}
    ${H_HEADER_FILES}
    ${SOURCE_CPP_FILES}
    ${SOURCE_CC_FILES}
    ${SOURCE_C_FILES}
    ${INL_HEADER_FILES}
)
